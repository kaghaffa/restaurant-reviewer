define([
  'react'
], function(React) {
  'use strict';

  return React.createClass({

    render: function() {
      return (
        <div className="restaurants-list-header">
        </div>
      );
    }
  });
});